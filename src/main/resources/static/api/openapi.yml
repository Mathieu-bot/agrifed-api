openapi: 3.2.0
info:
  title: Federation of Agricultural Collectivities - API
  version: 1.0.0
  description:
    API for managing the Federation of Agricultural Collectivities in Madagascar.
    This file contains reusable components (schemas, parameters, responses).

components:
  responses:
    #=================================
    # Schemas - Exception responses
    #=================================
    '400':
      description: Bad request - validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/BadRequestException'
    '401':
      description: Unauthorized - authentication required
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/UnauthorizedException'
    '403':
      description: Forbidden - insufficient permission
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NotAuthorizedException'
    '404':
      description: Not found - resource does not exist
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ResourceNotFoundException'
    '409':
      description: Conflict - data integrity issue
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ConflictException'
    '422':
      description: Unprocessable entity - business rule violation
      content:
        application/json:
          schema: '#/components/schemas/BusinessRuleViolationException'
    '429':
      description: Too many requests to the API
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TooManyRequestsException'
    '500':
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/InternalServerException'
    Exception:
      type: object
      properties:
        type:
          type: string
        message:
          type: string
    BadRequestException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: BadRequestException
        message: Bad request
    UnauthorizedException:
      allOf:
        - $ref: '##/components/schemas/Exception'
      example:
        type: UnauthorizedException
        message: Unauthorized
    NotAuthorizedException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: NotAuthorizedException
        message: Not authorized
    ResourceNotFoundException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: ResourceNotFoundException
        message: Resource of type <T> identified by <I> not found
    ConflictException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: ConflictException
        message: Data conflict
    BusinessRuleViolationException:
      allOf:
        - $ref: '#/#/components/schemas/Exception'
      example:
        type: BusinessRuleViolationException
        message: Business rule not respected
    TooManyRequestsException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: TooManyRequestsException
        message: Too many requests
    InternalServerException:
      allOf:
        - $ref: '#/components/schemas/Exception'
      example:
        type: InternalServerException
        message: Unexpected error